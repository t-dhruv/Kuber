# Kuber Audit Tracker

Keep this file small and focused on open quality debt, product gaps, and verification gaps. Do not add completed-work history here unless it explains an active risk.

## Tracking Rules

- Update this file after meaningful feature work, bug fixes, refactors, security hardening, or investigations that uncover gaps.
- Track only open debt, active risks, blocked verification, and next actions.
- Remove items once they are fixed and verified.
- Mirror any code TODO here, or remove the code TODO.
- Include verification gaps when tests, builds, lint, E2E, or smoke checks were skipped or blocked.

## Current Priority Debt

- **Modularity:** Continue breaking down oversized files, especially `client/src/pages/settings/tabs/SettingsSections.tsx` at about 1,345 lines, `client/src/pages/settings/components/CategoriesSection.tsx` at about 650 lines, `client/src/pages/transactions/TransactionsPage.tsx`, `client/src/pages/investments/InvestmentsSections.tsx`, `client/src/pages/accounts/AccountsPage.tsx`, and large server route/service modules.
- **Type safety:** Client lint is clean; backend lint reports 521 broad `any`/CommonJS warnings across services, route modules, lib helpers, and tests (verified 2026-07-31; the previously recorded 461 was stale). Prioritize production server modules before test mocks.
- **Exports/imports:** Add browser E2E coverage for authenticated report exports, Settings data exports, Settings Excel export, CSV import, and archive flows.
- **Report algorithms:** Continue the full Reports audit. Open findings from the first pass: legacy `/cashflow/sankey` does not honor old Reports page `excludeCategoryIds`/`excludeAccountIds` filters, and legacy investment widgets still need a TWR/MWR terminology audit. The fabricated retirement success percentage is resolved — `/investments/retirement-simulation` no longer reports a probability at all, because Kuber does not collect the inputs a real Monte Carlo needs (retirement age, target spending, withdrawal strategy). Reinstating one requires collecting those inputs first.
- **Reporting standardization:** Phase 1-3 backend foundation now exists under `/api/v1/reports/standard/*` for overview, income-vs-expense, cash-flow, category, budget, tag, account, merchant, audit/data-quality, net worth, investment summary, drill-down, options, standard CSV/JSON exports, and saved-report compatibility aliases. The Reports page now renders the standard contract for the main report tabs with household-scoped filter options, standard exports, standard drill-downs, URL filters, and saved report load/update/favorite support. Remaining reporting work: add cross-household integration coverage with a real database fixture, decide whether saved-report favorite/metadata warrants nullable columns instead of storing metadata in `filters`, split the large backend standard reporting module, and fully audit TWR/MWR return calculations before exposing performance-return claims.
- **Audit accuracy:** Reconcile older audit docs when the current code has already fixed a listed gap.
- **Backend structure:** Keep public route entry points thin and continue moving non-trivial business logic into focused services/lib modules.
- **Security/data integrity:** Keep checking household scoping, role checks, soft-delete behavior, secret redaction, SSRF protections, and encrypted credential storage during every related change. Investment records (holdings, lots, dividends, recurring schedules) and manual assets/liabilities are now soft-deleted; `Account`, `Transaction`, `Category`, `Budget` and `Goal` already were. When adding any new financial model, add `isDeleted` in the same change.
- **Security/auth roadmap:** Email verification, generalized MFA, email OTP MFA, admin MFA reset hardening, and the field-level encryption foundation are implemented. Remaining E2EE roadmap items: recovery-key setup/download/confirmation UX, household key unlock flow, migration UI for legacy plaintext account labels, encrypted transaction notes/content, and explicit AI opt-in before sending decrypted E2EE fields.
- **Dependency security:** `npm audit` reports 2 findings (both high) as of 2026-08-03, down from 5 (3 low, 2 high) and originally 24 (13 high). The two remaining highs are the *same* advisory reported twice (`react-router` and its `react-router-dom` parent), and there is **no available fix**:
  - react-router RSC Mode CSRF (`GHSA-qwww-vcr4-c8h2`), fixed in 8.3.0 which is unpublished (latest published is `react-router-dom` 7.18.2 / `react-router` 8.2.0). Only reachable in RSC mode; Kuber is a Vite SPA using the declarative API (`BrowserRouter`, `Routes`, `Route`, `Link`, `NavLink`, `Navigate`, `Outlet`, `useLocation`, `useNavigate`, `useSearchParams`) with no RSC, no SSR, and no data router (`RouterProvider`/`createBrowserRouter` are unused). Verified 2026-08-03.
  - **Do not "fix" this by downgrading.** `npm audit fix --force` proposes `react-router-dom@7.11.0`, which is strictly worse: 7.11.0 carries 14 advisories including a high-severity RCE via vendored turbo-stream deserialization (`GHSA-49rj-9fvp-4h2h`) and, critically, an open redirect through `<Link>`/`useNavigate` (`GHSA-wrjc-x8rr-h8h6`, fixed in 7.18.0) that *is* reachable from Kuber's declarative usage. 7.18.2 is the safest published version; stay on it and re-evaluate only when 8.3.0 ships.
  - esbuild dev-server arbitrary file read (`GHSA-g7r4-m6w7-qqqr`) is **fixed** as of 2026-08-03. The blocker was Kuber's own root `overrides` entry pinning `esbuild` to `^0.27.4`, not vite: vite 7.3.6 accepts `^0.27.0 || ^0.28.0`. Bumping the override to `^0.28.1` and running `npm update esbuild` (plain `npm install` will not re-resolve the locked version) resolves it with builds and tests green. The earlier note claiming this removed esbuild and broke the vite build was wrong.
  Recheck on every dependency change: this regressed once before (plan 002 fixed it, then it drifted back to 24).
- **E2E suite is stale, not just unwired:** the 21 Playwright specs now run in CI (PR #148) but only 3 of 13 `@smoke` tests pass. Failures are locator drift against the current UI, e.g. `getByRole('heading', { name: /accounts/i })` no longer matches on the accounts page. Auth and dashboard pass, so the harness is sound. Each spec needs auditing against current markup. PR #148 is deliberately left open rather than merged with `continue-on-error`, because a gate that cannot fail is indistinguishable from no gate.
- **Unapplied migrations:** `20260731090000_add_soft_delete_to_investments` and `20260731093000_add_soft_delete_to_manual_assets_liabilities` were written without a database available, so neither has been applied or rolled back locally. Run `make db-migrate` against a real database before release.

## Verification Gaps

- **E2E specs are stale** — 3 of 13 `@smoke` tests pass. See Current Priority Debt. Everything below that says "needs E2E coverage" is blocked behind repairing the existing specs first.
- The two soft-delete migrations from 2026-07-31 have not been applied against a real database.
- Settings Data export/archive actions need E2E coverage.
- Report PDF/CSV/Excel authenticated downloads need E2E coverage.
- Large frontend pages still need smoke or workflow coverage when they are split.
- Add E2E smoke coverage for email OTP MFA login and encryption status/settings display.
- The Excel export path (`server/src/routes/exports.ts`) has no automated test. The exceljs 3->4 major bump was verified with a one-off runtime smoke check instead; a real test belongs here.
- **Coverage scope is narrow and now honest about it:** `qa:gate` reports 96.11% over 25 of 154 server files (16.2%) and 100% over 3 of 162 client files (1.9%), with feature coverage at 53.85% (21 COVERED / 14 PARTIAL / 4 MISSING). `qa:release-gate` fails by design until real coverage catches up. Do not widen the ratchet baseline without widening the include lists.

## Latest Verification

- 2026-07-31: Audit-accuracy pass plus the open-risk backlog. Three finished-but-unmerged branches were landed (honest release gate, per-client rate limiting via `trust proxy`, removal of the `Math.random()` retirement success probability). Fixed the pre-push hook, which was mode 100644 and therefore silently ignored by git, so `qa:gate` had never run on any push. Dependency findings went 24 (13 high) -> 5; most collapsed to one root cause, `brace-expansion <=5.0.7`, reaching the tree through eslint/exceljs/workbox-build. Re-did plan 004 for real: investment holdings, lots, dividends and recurring schedules are now soft-deleted across 72 read sites, closing a live data-loss path where deleting an account hard-deleted its holdings and cascade-wiped all trade history and cost basis. Stopped reading stale Decimal shadow columns for balances and holdings (plan 021) — no runtime code writes them, so the account list and net-worth report had been showing migration-day values. Soft-deleted manual assets/liabilities (plan 022). Dropped `household_id` Prometheus labels and bounded the unauthenticated logo endpoint with a 512KB response cap, negative caching, input length limits and a dedicated rate limiter. Verification run: `npm run test` (734 passing: 675 server / 59 client), `npm run build` (both workspaces), `npm run lint` (0 errors, 521 server warnings), `npm run qa:gate` (ratchet passed), `npx prisma validate`. New soft-delete and Decimal-freshness tests were mutation-checked by reintroducing each bug and confirming they fail. Notable process finding: in three separate cases the existing test asserted the buggy behaviour (`investmentHolding.deleteMany` was called, `manualAsset.delete` was called, net-worth fixtures set only the Decimal with the Float at 0), so the tests codified the bugs rather than catching them. Verification gaps: two migrations written without a database available and not yet applied; E2E specs remain stale (see Verification Gaps).

- 2026-05-26: Fixed shared modal scrolling by bounding modal height, clipping the modal shell, and making modal content scroll independently with sticky footers, so long rule builders keep save/cancel actions reachable without page-level modal scrolling. Fixed rule All/Any matching in remaining rule application paths (`ruleMatches`, batch auto-categorize, import suggestion paths) so non-strict rules can match any condition consistently. Added direct Create rule support from transaction review rows and normal transactions, with tested transaction-derived prefill, and made the transaction review screen full width with responsive header/actions/row controls. Verification run: `rtk npm run test --workspace=server -- ruleEngine.test.ts`, `rtk npm run test --workspace=@kuber/client -- reviewRulePrefill.test.ts transactionRulePrefill.test.ts`, `rtk npm run build --workspace=server`, `rtk npm run lint --workspace=@kuber/client`, and `rtk npm run build --workspace=@kuber/client`. Client build still reports existing large-chunk warnings.
- 2026-05-26: Added the Phase 1 standard reporting backend foundation: normalized report query validation, unified response contract, registry-based generators for Overview, Income vs Expense, and Cash Flow, Decimal-backed calculations, split-level category totals, tag multi-count warnings, household-scoped standard journal loader, paginated drill-downs, `/reports/standard/options`, `/reports/standard/generate`, `/reports/standard/preview`, `/reports/standard/drilldown`, and saved-report `/reports/saved-views` aliases plus PUT update support using the existing `SavedReport` model. Existing legacy report endpoints remain in place. Verification run: `rtk npm --workspace server run test -- reports.test.ts reportingStandard.test.ts` and `rtk npm --workspace server run build`.
- 2026-05-26: Completed Phase 2 backend standard reporting coverage for Category, Budget, Tag, Account, and Merchant reports. Added combined filter support across category/account/tag/merchant, split-category drill-down with `matchedAmountDecimal`, BudgetLimit-backed budget utilization, tag multi-count warning behavior, and `/reports/standard/export/csv` plus `/reports/standard/export/json`. Existing legacy reports remain in place. Verification run: `rtk npm --workspace server run test -- reports.test.ts reportingStandard.test.ts`, `rtk npm --workspace server run build`, and `rtk npm --workspace server run test`.
- 2026-05-26: Completed Phase 3 backend standard reporting coverage for Audit/Data Quality, Net Worth, and a safe Investment summary. Audit report now returns actionable issue rows with drill-down metadata and uses existing duplicate/import/rule/excluded-account/category tables. Net worth report uses existing `NetWorthSnapshot`, `Account`, `ManualAsset`, `ManualLiability`, and `InvestmentHolding` data. Investment report is intentionally limited to holdings/current value/cost basis/unrealized gain-dividend summary and returns a warning that realized performance is not yet audited. Verification run: `rtk npm --workspace server run test -- reports.test.ts reportingStandard.test.ts`, `rtk npm --workspace server run build`, and `rtk npm --workspace server run test`.
- 2026-05-26: Wired the Reports page to the standard reporting contract for the primary report tabs, including `/reports/standard/generate`, `/reports/standard/drilldown`, `/reports/standard/export/csv`, `/reports/standard/export/json`, real household-scoped `/reports/standard/options` filter payloads, saved report selector/load/update/delete/favorite UI, and URL-backed filters. Standard investment reporting now includes realized gain/loss only from confirmed sell lots with stored `realizedGainDecimal`, and still avoids TWR/MWR return claims until cash-flow timing is audited. Verification run: `rtk npm --workspace client run test -- standardReportClient.test.ts dateRange.test.ts`, `rtk npm --workspace client run test`, `rtk npm --workspace client run build`, `rtk npm --workspace server run test -- reports.test.ts reportingStandard.test.ts`, `rtk npm --workspace server run build`, and `rtk npm --workspace server run test`. Client build still reports existing large-chunk warnings.
- 2026-05-25: Fixed production transaction/account issues from the triage pass: account detail history and account transaction lists now use the selected account's journal entry amount, transaction search/filter query construction now composes search with cursor pagination and supports multiple category filters plus uncategorized-only filtering, CSV/import webhook paths now apply active rules immediately after creating full journal records, duplicate review no longer skips items after each action and has a dismiss-all action, Rules page uses full-width responsive layout, and API/auth rate limits are configurable with higher single-user defaults documented in `.env.example` and `docs/03-reference.md`. Verification run: `rtk npm run test --workspace=server -- tests/services/transactionService.test.ts tests/routes/accountEntryAmount.test.ts`, `rtk npm run build --workspace=server`, `rtk npm run build --workspace=@kuber/client`, and `rtk npm run test --workspace=server -- tests/routes/accounts.test.ts tests/routes/transactions.test.ts tests/routes/import.test.ts tests/routes/duplicates.test.ts tests/routes/rules.test.ts tests/services/transactionService.test.ts tests/routes/accountEntryAmount.test.ts`. Client build still reports existing large-chunk warnings.
- 2026-05-25: Started the full report algorithm audit across Reports tabs and report-style endpoints. Fixed 50/30/20 wealth analysis so positive `withdrawal` journal amounts count as spending buckets, and fixed budget variance to exclude soft-deleted budgets. Remaining report-algorithm audit debt is tracked under Current Priority Debt. Verification run: `rtk npm --prefix server run test -- tests/routes/reports.test.ts tests/routes/wealth.test.ts tests/routes/wealthRoutes.test.ts --run`, `rtk npm --prefix server run test -- tests/routes/cashflow.test.ts tests/routes/reports.test.ts tests/routes/wealth.test.ts tests/routes/wealthRoutes.test.ts tests/routes/investments.test.ts tests/routes/networth.test.ts tests/routes/taxAccounts.test.ts --run`, and `rtk npm --prefix server run build`.
- 2026-05-25: Fixed cashflow/report direction handling so journal withdrawals with positive `amountDecimal` count as expenses across yearly cashflow, monthly cashflow, Sankey, and forecast history; transfer journals are excluded at the cashflow query boundary; report filter aliases from the Reports page now resolve to the server's canonical `excludeCategoryIds`/`excludeAccountIds` filters. Verification run: `rtk npm run test --workspace=@kuber/server -- tests/routes/cashflow.test.ts tests/routes/reports.test.ts`, `rtk npm run test --workspace=@kuber/server -- tests/lib/journalReportingCore.test.ts tests/lib/reportCashFlow.test.ts tests/lib/reportingCore.test.ts tests/lib/reportOverview.test.ts tests/lib/reportRules.test.ts`, `rtk npm run test --workspace=@kuber/server -- tests/lib/reportDiagnostics.test.ts tests/lib/reportInvestments.test.ts tests/lib/reportOverview.test.ts tests/lib/reportRules.test.ts tests/lib/reporting/freshness.test.ts tests/lib/reporting/snapshots.test.ts tests/lib/reporting/rollups.test.ts tests/lib/reporting/periods.test.ts tests/lib/reporting/classification.test.ts tests/routes/reports.test.ts tests/routes/cashflow.test.ts`, `rtk npm run build --workspace=@kuber/server`, and `rtk npm run build --workspace=@kuber/client`. Client build still reports existing large-chunk warnings.
- 2026-05-25: Added a quick inline category type selector in Settings > Categories so `income`, `expense`, and `transfer` can be changed without opening the edit modal. Reused the existing settings category update API with optimistic client cache updates. Verification run: `rtk npm run lint --workspace=client` and `rtk npm run build --workspace=client`. Root `rtk npm run typecheck` was attempted first but the root package has no `typecheck` script. Client build still reports existing large-chunk warnings.
- 2026-05-25: Fixed rules bulk execution so `Apply all` includes active ungrouped rules in addition to grouped rules. Added rules JSON export/import API support and Rules page import/export controls; exports omit database ownership identifiers and imports create rules under the authenticated household. Verification run: `rtk npm run test --workspace=server -- tests/routes/rules.test.ts`, `rtk npm run build --workspace=server`, and `rtk npm run build --workspace=@kuber/client`. Client build still reports existing large-chunk warnings.
- 2026-05-25: Removed category-level `bucketType: "uncategorized"` assignment paths. Default categories now declare only `needs`, `wants`, or `savings` buckets and signup/demo seeding persists those buckets; custom category APIs and the Settings bucket UI reject/remove the `uncategorized` option; the Prisma default and migration backfill existing uncategorized category buckets to `wants`. Transaction rows without a category still report as uncategorized spending. Verification run: `rtk npm --prefix server run test -- tests/lib/defaultCategories.test.ts tests/routes/categories.test.ts tests/routes/settings.test.ts tests/routes/wealthRoutes.test.ts`, `rtk npx prisma format`, `rtk npm --prefix server run build`, and `rtk npm --prefix client run build`. Client build still reports existing large-chunk warnings.
- 2026-05-25: Reworked category typing so `Category.type` is the single required source of truth (`income`, `expense`, or `transfer`) and `CategoryGroup` is now an untyped grouping label. Added a migration to normalize existing category types, backfill report-sensitive mappings such as emergency fund/RRSP/TFSA/debt principal as transfers and ABM cash/e-transfer sent as expenses, enforce a database check constraint, and drop `category_groups.type`; aligned signup/default/demo category seeding, settings category/group APIs, budget category grouping, shared DTO typing, Settings categories UI, and cashflow/report transfer filtering. Verification run: `rtk npm --prefix server run test -- tests/prisma/seed.test.ts tests/lib/journalReportingCore.test.ts tests/routes/reports.test.ts tests/routes/cashflow.test.ts --run`, `rtk npm --prefix server run test -- tests/routes/settings.test.ts tests/routes/categories.test.ts tests/routes/budgets.test.ts --run`, `rtk npx --prefix server prisma format --schema server/prisma/schema.prisma`, `rtk npm --prefix server run db:generate`, `rtk npm --prefix server run build`, and `rtk npm --prefix client run build`. Client build still reports existing large-chunk warnings.
- 2026-05-22: Fixed full-page refresh session restoration on protected client routes. Protected routes now wait for the refresh-cookie flow to reissue an in-memory access token and reload `/users/me` before redirecting to login, so reloads no longer race the auth guard. Added a focused client unit test for session restoration. Verification run: `rtk npm run test --workspace=@kuber/client -- tests/lib/api.test.ts tests/stores/authStore.test.ts`, `rtk npm run build --workspace=@kuber/client`, and `rtk npm run lint --workspace=@kuber/client`.
- 2026-05-22: Seed demo account now sets `emailVerifiedAt` so `demo@kuber.app` can sign in after database seeding without an email-verification step. Added a focused seed fixture test and made the seed module import-safe for tests. Also checked the recent root `.env` loader change against Docker Compose: dev Compose already provides explicit container environment, and prod Compose still uses Compose interpolation plus `env_file: .env`, so no Compose file change was needed. Verification run: `npm run test --workspace=server -- tests/prisma/seed.test.ts`, `cd server && npx tsc --noEmit`, `docker compose config`, and `docker compose --env-file <temp> -f docker-compose.prod.yml config --no-env-resolution`. Note: `npm run typecheck --workspace=server` was attempted first but that workspace has no `typecheck` script.
- 2026-05-22: Added a consistent root env-loading wrapper for local Make, root npm, and workspace npm app/build/test/database commands. `scripts/run-with-root-env.mjs` loads repo-root `.env` with Node's dotenv parser, preserves explicitly exported shell variables, and can run commands from repo, server, or client working directories so subdirectory commands inherit the same environment. Verification run: `node scripts/run-with-root-env.mjs node -e '...'` confirmed root `DATABASE_URL`/`JWT_SECRET` loading, `DATABASE_URL=from-shell node scripts/run-with-root-env.mjs node -e '...'` confirmed shell overrides win, `make -n dev-server build-server test-server db-migrate start` confirmed Make targets route through the wrapper, `make db-env-check` passed, `node -e 'JSON.parse(...)'` confirmed package JSON syntax, `npm pkg get ...` confirmed root/server/client npm scripts route through the wrapper, `node scripts/run-with-root-env.mjs --cwd server/client node -e '...'` confirmed working-directory handling, and `npm run db:generate --workspace=server` passed. Note: `rtk` was unavailable in this shell, so the underlying commands were run directly.
- 2026-05-22: Fixed `make db-reset` so Make database targets export `DATABASE_URL` from the repo-root `.env` when it is not already present in the shell. Root cause was Prisma being invoked from `server/`, where it could not auto-load the root `.env`. Verification run: `make db-reset` completed reset, migrate, generate, and seed successfully. Note: `rtk` was unavailable in this shell, so the underlying command was run directly.
- 2026-05-21: Implemented Phase 1 email verification foundation: `User.emailVerifiedAt`, hashed `SecurityToken` storage for email verification/password reset, verification/resend auth endpoints, unverified-login block, invite signup pending-verification behavior, and client verification flow. Verification run: `npx prisma format --schema server/prisma/schema.prisma`, `npm run db:generate --workspace=server`, `npm run test --workspace=server -- tests/lib/securityTokens.test.ts tests/routes/authEmailVerification.test.ts tests/routes/authResetToken.test.ts tests/routes/auth2fa.test.ts`, `npm run build`, `npm run test`. Note: `rtk` was unavailable in this shell, so the underlying commands were run directly.
- 2026-05-22: Pre-push safety check removed generated local build/cache artifacts, removed tracked generated Superpowers planning docs from the branch, and fixed the stale household invite/member MFA reset route test mock so it exercises the transactional reset path. Verification run: `npm run test --workspace=server -- tests/routes/householdInvites.test.ts`, `npm run test`, `npm run lint`, `npx prisma generate --schema server/prisma/schema.prisma`, `npm run build`, and GitNexus change detection. Note: `rtk` was unavailable in this shell, so the underlying commands were run directly.
- 2026-05-21: Completed pending security foundation work from `docs/superpowers/specs/2026-05-21-security-foundation-e2ee-design.md`: generalized MFA responses, email OTP challenge/verify/enable/disable, MFA settings UI, owner member-MFA reset hardening with session invalidation and audit logging, household encryption metadata/API, WebCrypto helpers, encrypted account label storage/display fallback, and encryption settings status. Verification run: `npm run test --workspace=server -- tests/lib/securityTokens.test.ts tests/routes/authEmailVerification.test.ts tests/routes/authResetToken.test.ts tests/routes/auth2fa.test.ts tests/routes/authMfaEmail.test.ts tests/routes/settings.test.ts tests/lib/encryptedField.test.ts tests/routes/securityEncryption.test.ts tests/routes/accounts.test.ts`, `npm run test --workspace=client -- tests/pages/loginMfa.test.tsx tests/lib/security/webCrypto.test.ts tests/pages/accounts/accountEncryption.test.ts`, `npm run build`, and `npx gitnexus analyze`.
- 2026-05-21: Production backend helpers `transactionJournalService.ts`, `journalReportingCore.ts`, `priceCache.ts`, `reporting/rollups.ts`, `reporting/snapshots.ts`, `pdfParser.ts`, `ai/index.ts`, `softDeleteWhere.ts`, `audit.ts`, `default-categories.ts`, `webhookFire.ts`, `routes/audit.ts`, and `routes/logos.ts` are lint-clean. `rtk npm run build`, `rtk npm run test`, and `rtk npm run lint` pass; root lint still reports 438 server warnings.
- 2026-05-21: Security foundation and field-level E2EE design documented. No runtime tests run because this slice only added planning documentation.

## Quality Categories To Track

- Security and auth/access control
- Household scoping and data integrity
- Soft delete and retention behavior
- Exports, imports, and reports
- Modularity and oversized files
- Type safety and API contracts
- Tests, E2E, smoke checks, and coverage gaps
- Accessibility and UX reliability
- Performance and bundle size
- Deployment and documentation
