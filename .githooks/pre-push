#!/bin/sh
set -eu

# This hook used to run the coverage ratchet, which was dropped along with the
# rest of the old script layer. It now runs the checks that must hold on every
# push and that CI will re-run anyway.

echo "Running lint..."
npm run lint

echo "Running tests..."
npm test
