/**
 * Shared fixture data. Kept out of the spec files so that one spec can refer to
 * another's data without importing a module that registers tests.
 */
export const ACCOUNT_NAME = 'Main Chequing';
export const MERCHANT = 'Corner Grocer';
